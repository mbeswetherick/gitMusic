// js/views/todos.js

  var app = app || {};

  app.UserView = Backbone.View.extend({

    el: '#holder',


    // Re-renders the titles of the todo item.
    render: function() {
      $p = $("<p>duba spew</p>");
      console.log('horse seekers');
      //this.$el.append($p);
    },

    

  
});